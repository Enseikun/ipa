# レクチャーノート：コンピュータ構成要素 ― 2.2 メモリアーキテクチャ

対応範囲：応用情報技術者試験・情報処理安全確保支援士試験（午前I／午前II共通）「コンピュータシステム」＞「コンピュータ構成要素」＞「メモリ」

## 0. 全体像 ― なぜこの分野を学ぶのか

前回のノートでは、プロセッサが「命令を読み出し、演算する」頭脳の役割を担うことを見ました。しかし頭脳がいくら速くても、命令やデータを蓄えておく「記憶（メモリ）」が遅ければ、システム全体としては速くなりません。本ノートは、①メモリを構成する半導体素子の種類（情報素子）、②プロセッサとメモリの速度差を埋める仕組み（キャッシュを中心としたメモリシステムの構成）、③記憶した内容が壊れていないかを確認する仕組み（誤り検出方式）、という3段階のストーリーで説明します。

出題傾向としては、キャッシュメモリのヒット率や平均アクセス時間の計算問題、ECC・パリティ・ハミング符号による誤り検出/訂正の仕組みが、応用情報の午前試験で計算問題・知識問題として繰り返し出題される定番論点です。

## 1. 情報素子 ― メモリを作る半導体の種類

### 1.1 半導体メモリとその大分類

現在のコンピュータの主記憶・補助記憶の多くは**半導体メモリ**、すなわち半導体素子を用いて情報を記憶する部品で構成されています。半導体メモリは、電源を切っても記憶内容が消えるかどうかで大きく2つに分かれます。

- **ROM（Read Only Memory）**：基本的に読み出し専用のメモリで、電源を切っても記憶内容が消えない**不揮発性**のメモリです。BIOSやファームウェアなど、書き換える必要のない（あるいは頻繁には書き換えない）データの格納に使われます。
- **RAM（Random Access Memory）**：自由に読み書きできるメモリですが、電源を切ると記憶内容が消えてしまう**揮発性**のメモリです。プログラムの実行中に使うデータや命令を一時的に記憶する主記憶として使われます。

「ROM＝不揮発性・読み出し中心」「RAM＝揮発性・読み書き自由」という対応関係は基本中の基本ですが、試験では確実に問われるため、まずここを押さえます。

### 1.2 素子の物理的な仕組み：バイポーラ型とMOS型

半導体メモリを構成するトランジスタの動作原理には、大きく2つの系統があります。

- **バイポーラ型**：電子と正孔（ホール）の両方のキャリアを利用して動作するトランジスタ。動作は高速ですが、消費電力が大きいという特徴があります。
- **MOS型（Metal Oxide Semiconductor）**：電圧によって電流の通り道（チャネル）の開閉を制御するトランジスタ。バイポーラ型に比べて消費電力が小さく、高集積化に向いているため、現在の半導体メモリの主流はMOS型です。

さらにMOS型の中でも、次のような構成があります。

- **CMOS（Complementary MOS）**：極性の異なる2種類のMOSトランジスタ（P型とN型）を相補的に組み合わせた回路構成。信号が変化する瞬間以外はほとんど電流が流れないため消費電力が非常に小さく、現在の集積回路（プロセッサやメモリ）の主流の構成方式です。
- **BiCMOS（Bipolar-CMOS）**：バイポーラ型の高速性とCMOSの低消費電力性を両立させるために、両方の素子を1つのチップ上に混在させた構成方式です。

「バイポーラ＝速いが電力大」「MOS（特にCMOS）＝電力小で高集積」という対比、そしてその折衷案としてのBiCMOSという位置づけを理解しておくと整理しやすい論点です。

### 1.3 SRAMとDRAM：RAMの2つの実現方式

前述のRAM（揮発性の読み書き可能メモリ）には、内部の記憶方式が異なる2つの種類があります。

- **SRAM（Static RAM）**：フリップフロップ（前回のノートで扱った、状態を保持できる回路）を用いて情報を記憶する方式。電源が入っている限り、放っておいても記憶内容が消えないため「静的（Static）」と呼ばれます。構造がやや複雑でコストが高い分、高速に動作するため、後述する**キャッシュメモリ**に使われます。
- **DRAM（Dynamic RAM）**：コンデンサ（キャパシタ）に電荷を蓄えることで情報を記憶する方式。コンデンサは時間が経つと自然に放電してしまうため、記憶内容を保持するために定期的に電荷を補充する**リフレッシュ動作**が必要になります。これが「動的（Dynamic）」と呼ばれる理由です。構造がSRAMより単純で高集積・低コストにできるため、コンピュータの主記憶（メインメモリ）として広く使われます。

まとめると、「SRAM＝フリップフロップ・リフレッシュ不要・高速・高コスト・キャッシュ用途」「DRAM＝コンデンサ・リフレッシュ必要・低速・低コスト・主記憶用途」という対比がそのまま試験の頻出パターンです。

### 1.4 SoC（System on a Chip）

**SoC（System on a Chip）**は、プロセッサ・メモリ・入出力インタフェースなど、コンピュータシステムとして必要な複数の機能を1つの半導体チップ上に統合したものです。複数の部品を個別に基板上に配置するよりも、小型化・省電力化・低コスト化が図れるため、スマートフォンや組込み機器など、小型で省電力性が求められる機器で広く採用されています。

### 1.5 磁性体メモリ

これまで見てきたRAM・ROMは半導体（電荷や電流の状態）で情報を記憶する方式でしたが、**磁性体メモリ**は、物質の磁化の向き（磁気的な性質）を利用して情報を記憶する方式です。代表例としてMRAM（Magnetoresistive RAM）などがあり、電源を切っても記憶内容が消えない不揮発性を持ちながら、DRAM並みの高速な読み書きが可能という特徴を持つ、次世代の不揮発性メモリとして位置づけられます。半導体メモリとは異なる原理で情報を記憶する方式として、対比的に押さえておきましょう。


## 2. メモリシステムの構成 ― キャッシュという発想

### 2.1 なぜキャッシュが必要か

プロセッサの動作速度は年々向上してきましたが、主記憶（DRAM）のアクセス速度はそれほど速くならず、両者の間には大きな速度差（ボトルネック）が生まれています。この速度差を埋め、プロセッサを待たせないようにする仕組みが**キャッシング**という発想であり、それを実現する高速な記憶装置が**キャッシュメモリ**です。

キャッシュメモリは、前述の**SRAM**という高速だが高コストな素子で構成され、プロセッサと主記憶の間に配置されます。プロセッサが一度アクセスしたデータや、その近くのデータをあらかじめキャッシュメモリに複製しておくことで、次に同じデータやその近辺にアクセスする際は低速な主記憶ではなく高速なキャッシュメモリから読み書きできるようにする、というのが**キャッシュメモリの役割**です。

同様の「よく使うものを高速な場所に複製しておく」という考え方は、記憶階層のさらに下位、すなわち低速な補助記憶装置（ハードディスクなど）と主記憶の間にも応用されており、これを**ディスクキャッシュ**と呼びます。「キャッシュ」という考え方自体は特定の階層に限らず、速度差のあるあらゆる記憶階層間で使われる汎用的な仕組みである、という点を理解しておくと応用が利きます。

### 2.2 書き込み方式：ライトスルーとライトバック

プロセッサがキャッシュメモリ上のデータを書き換えたとき、その変更を主記憶（本来のデータ）にいつ反映するかによって、2つの方式があります。

- **ライトスルー方式**：キャッシュへの書き込みと同時に、主記憶へも即座に書き込みを行う方式。キャッシュと主記憶の内容が常に一致しているため単純で信頼性が高い一方、書き込みのたびに低速な主記憶へのアクセスが発生するため、書き込み性能はあまり向上しません。
- **ライトバック方式**：書き込みはまずキャッシュメモリにのみ行い、そのデータがキャッシュから追い出される（他のデータに置き換えられる）タイミングで、まとめて主記憶へ反映する方式。主記憶への書き込み回数を減らせるため書き込み性能は高い一方、キャッシュと主記憶の内容が一時的に不一致になる、構造がやや複雑になるという特徴があります。

「ライトスルー＝即座に反映・単純・低速」「ライトバック＝後でまとめて反映・高性能・複雑」という対比で覚えると整理しやすい論点です。

### 2.3 キャッシュメモリの割り当て方式（マッピング方式）

主記憶上の各データ（ブロック）を、容量の小さいキャッシュメモリのどの場所に対応させるかを決める規則を**キャッシュメモリの割り当て方式**と呼び、代表的に次の3種類があります。

- **ダイレクトマッピング方式**：主記憶上の各ブロックの格納先を、あらかじめ計算式（アドレスの一部）によって、キャッシュ上の**特定の1か所のみ**に固定して割り当てる方式。構成が単純で高速に判定できる一方、異なる主記憶ブロックが同じキャッシュ場所を取り合ってしまう（衝突による性能低下）が起きやすいという弱点があります。
- **フルアソシアティブ方式**：主記憶上のどのブロックも、キャッシュ上の**空いているどの場所にでも**自由に格納できる方式。衝突が起きにくく柔軟性が高い一方、格納されている場所を探すために全体を検索する必要があり、判定回路が複雑・高コストになります。
- **セットアソシアティブ方式**：上記2つの折衷案で、キャッシュをいくつかの組（セット）に分割し、各主記憶ブロックは「特定の1つのセットの中の、複数ある場所のどこか」に格納できるようにする方式。ダイレクトマッピング（自由度が低いが単純）とフルアソシアティブ（自由度は高いが複雑）の中間的な性質を持ち、実用上最も広く採用されている方式です。

これらの方式で、キャッシュ内の格納場所を「アドレスの値そのものではなく、格納されている内容（タグ）と照合して探し出す」仕組みを**連想記憶（アソシアティブメモリ）**と呼びます。フルアソシアティブ方式・セットアソシアティブ方式は、この連想記憶の仕組みを利用して、キャッシュ内を検索します。

### 2.4 ヒット率と平均アクセス時間

キャッシュメモリの効果を測る指標として重要なのが**ヒット率**です。プロセッサがデータにアクセスしようとしたとき、目的のデータがキャッシュメモリ上に存在している（ヒットする）割合を指します。ヒット率が高いほど、低速な主記憶へアクセスする頻度が減り、システム全体が高速化されます。

この考え方を数値化したものが**平均メモリアクセス時間**（および、それを踏まえた**平均命令実行時間**）です。キャッシュメモリのアクセス時間を$T_c$、主記憶のアクセス時間を$T_m$、ヒット率を$h$とすると、平均メモリアクセス時間は次の式で近似的に表されます。

$$
\text{平均アクセス時間} = h \times T_c + (1 - h) \times T_m
$$

つまり「ヒットしたときはキャッシュの速さで、ミスしたとき（ヒットしなかったとき）は主記憶の速さでアクセスすることになる」という考え方を、ヒット率で重みづけして平均を取ったものです。応用情報の午前試験では、この式を使ってヒット率や平均アクセス時間を求める計算問題が定番として出題されます。命令実行全体で見た場合の同様の考え方が**平均命令実行時間**で、キャッシュのヒット率を反映して命令の実行にかかる平均時間を求める場合に用いられます。

### 2.5 NFP（Near Fit Processor…ではなく「置き換えアルゴリズム」の文脈で問われる用語）

**NFP**は、キャッシュメモリ（や仮想記憶のページ置き換え）において、新しいデータを格納する際に、既存のどのデータを追い出すかを決める**置き換えアルゴリズム**に関連して登場する用語の一つとして、試験の選択肢に紛れ込むことがあります。キャッシュがいっぱいの状態で新しいブロックを読み込む必要が生じたとき、「どのデータを追い出すか」という置き換え方針（LRU：最も長く使われていないものを追い出す、FIFOなど）の一般的な枠組みの中で扱われる考え方として、置き換えアルゴリズム全般の存在とあわせて押さえておきましょう。

### 2.6 メモリインタリーブ

主記憶へのアクセスを高速化するもう一つの工夫が**メモリインタリーブ**です。主記憶を複数の独立したバンク（区画）に分割しておき、連続したアドレスへのアクセスを複数のバンクに分散させることで、あるバンクがアクセス中（動作中）でも別のバンクへ並行してアクセスを開始できるようにし、実効的なアクセス速度を向上させる技術です。キャッシュメモリが「よく使うデータを高速な場所に複製する」というアプローチであるのに対し、メモリインタリーブは「主記憶自体への物理的なアクセスを並列化する」というアプローチである点が対照的です。


## 3. 主記憶の誤り検出方式

半導体メモリは、宇宙線などの外的要因やハードウェアの微細な不具合によって、記憶している値が意図せず変化してしまう（ビット化けする）ことがあります。これを検出、あるいは訂正する仕組みが必要になります。

### 3.1 パリティチェック方式

最も基本的な誤り検出方式が**パリティチェック方式**です。データのビット列に対して、1の個数が偶数（または奇数）になるように調整するための1ビットの**パリティビット**を付加しておきます。データを読み出す際、実際の1の個数の偶奇とパリティビットが示す偶奇が一致しているかを確認することで、1ビットの誤りを**検出**できます。ただし、パリティチェックは誤りを検出できるだけで、どのビットが誤っているかを特定して**訂正**することはできません。また、偶数個のビットが同時に誤った場合は検出すらできない、という限界もあります。

### 3.2 ハミング符号

パリティチェックの「検出はできても訂正はできない」という限界を克服するのが**ハミング符号**です。データに対して複数のパリティビットを、特定の規則に基づいた位置に配置することで、誤りが発生した際に「どのビットが誤っているか」まで特定できるようにし、1ビットの誤りであれば自動的に**訂正**することが可能になります（一般的な構成では、1ビット誤りの訂正に加えて2ビット誤りの検出も可能です）。

### 3.3 ECC（Error Correcting Code）

**ECC**は、ハミング符号のような誤り訂正符号を利用して、主記憶などの記憶装置に誤り検出・訂正機能を持たせる仕組み全般を指す総称です。サーバ用のメモリなどでは「ECCメモリ」として、ハミング符号を用いた誤り訂正の仕組みが標準的に組み込まれており、稼働中に発生した1ビット程度の記憶誤りであれば、システムを停止させることなく自動的に訂正して動作を継続できます。

「パリティチェック＝検出のみ・訂正不可・1ビット追加」「ハミング符号（＝ECCの代表的な実現手段）＝検出＋訂正が可能・複数ビット追加」という対比の理解が、この単元の核心です。


## 4. 試験対策まとめ（重要ポイント）

- **ROM（不揮発性）とRAM（揮発性）**の対比、さらにRAMの内訳として**SRAM（フリップフロップ・リフレッシュ不要・高速高コスト・キャッシュ用途）とDRAM（コンデンサ・リフレッシュ必要・低速低コスト・主記憶用途）**の対比はセットで確実に暗記する。
- **バイポーラ型（速いが高消費電力）とMOS型（低消費電力・高集積、主流）**、およびその発展形である**CMOS（相補型で超低消費電力）・BiCMOS（両者の折衷）**の関係を押さえる。
- **キャッシュメモリの割り当て方式**：ダイレクトマッピング（単純・衝突しやすい）／フルアソシアティブ（自由・複雑）／セットアソシアティブ（折衷・実用上主流）の3方式の特徴の違いは頻出。
- **ライトスルー方式（即時反映・単純）とライトバック方式（遅延反映・高性能）**の違いと、それぞれのメリット・デメリットを対比で理解する。
- **平均メモリアクセス時間の計算式**（ヒット率で重みづけした加重平均）は、数値を当てはめる計算問題として頻出。式の意味（ヒット時とミス時の重みづけ平均）を理解しておけば初見の数値にも対応できる。
- **パリティチェック方式（検出のみ）とハミング符号／ECC（検出＋訂正）**の違いは、この単元で最も問われやすい対比。「訂正までできるかどうか」がキーワード。
- **SoC**は複数機能を1チップに統合する技術、**磁性体メモリ**は半導体とは異なる磁気的原理による不揮発性メモリ、という基本的な位置づけを押さえておけば十分。

---

*本ノートは「基本情報／応用情報技術者試験」の出題範囲（コンピュータ構成要素＞メモリ）に基づき作成しています。プロジェクト内の「出題傾向」「テキスト優先順位」ドキュメントによれば、支援士試験ではセキュリティ・ネットワークが最優先分野であり、本分野（コンピュータ構成要素）はA-1（共通知識）で1問前後の出題にとどまります。特にキャッシュメモリの計算問題と誤り訂正方式の違いを優先的に押さえ、深追いはせず典型論点の理解にとどめる学習配分が効率的です。*
