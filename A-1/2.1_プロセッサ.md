# レクチャーノート：コンピュータ構成要素 ― 2.1 プロセッサ

対応範囲：応用情報技術者試験・情報処理安全確保支援士試験（午前I／午前II共通）「コンピュータシステム」＞「コンピュータ構成要素」＞「プロセッサ」

## 0. 全体像 ― なぜこの分野を学ぶのか

コンピュータは大きく「プロセッサ（演算・制御を担う頭脳）」「メモリ（記憶）」「バス（データの通り道）」「入出力（外界とのやり取り）」の4要素で構成されます。本ノートはその中心である**プロセッサ**を、①基本構造と動作、②高速化の工夫、③複数個並べたときの話（マルチプロセッサ）、④その土台にある論理回路、という4段階のストーリーで説明します。

出題頻度としては「コンピュータ構成要素」自体はA-1（共通知識）で1問前後と厚くはありませんが、CISC/RISC・パイプライン・キャッシュ・アムダールの法則などは応用情報の午前で計算問題としても繰り返し出題される定番論点であり、午後の性能計算問題の基礎にもなるため、軽視せず押さえておく価値があります。

## 1. プロセッサの構造と動作原理

### 1.1 プロセッサとは何か

**プロセッサ**（CPU：Central Processing Unit）は、主記憶（メモリ）から命令とデータを読み出し、演算・制御を行う装置です。「プログラム内蔵方式（ノイマン型）」の考え方に基づき、命令をメモリに置いておき、プロセッサが順番に取り出して実行する、という仕組みが現代のコンピュータの基本です。

### 1.2 プロセッサの構成

プロセッサは主に次の要素から成り立っています。

- **制御装置（コントロールユニット）**：命令を解読し、各装置に制御信号を送る。
- **演算装置（ALU：Arithmetic Logic Unit）**：加減算や論理演算を実際に行う。
- **レジスタ群**：演算対象や中間結果、制御情報を一時的に保持する高速な記憶場所。プログラムカウンタ（次に実行する命令のアドレスを保持）、命令レジスタ、汎用レジスタ、後述するPSWなどが含まれる。

動作の基本サイクルは「命令フェッチ（読み出し）→命令デコード（解読）→実行（演算・アドレス計算など）→結果の書き戻し」という**命令サイクル**の繰り返しです。この基本サイクルをどう速く回すかが、後述する高速化方式（パイプラインなど）の主題になります。

### 1.3 VLIW

**VLIW（Very Long Instruction Word）**は、複数の演算を1つの長い命令語にまとめてコンパイラが事前にスケジューリングし、プロセッサ側は単純にそれを並列実行するという設計方式です。後述するスーパスカラ方式がハードウェアで動的に命令の並列実行を判断するのに対し、VLIWはコンパイラが静的に（実行前に）並列実行の組み合わせを決めておく点が対照的です。ハードウェアを単純化できる反面、コンパイラの最適化能力に性能が大きく依存します。

### 1.4 割り込み

プロセッサは通常、プログラムを順番に実行しますが、緊急の処理が必要になったときに実行中の処理を一時中断し、別の処理（割り込み処理ルーチン）に制御を移す仕組みが**割り込み**です。割り込みは発生要因によって大きく2つに分類されます。

- **外部割り込み**：プロセッサの外部で発生する要因による割り込み。入出力装置からの完了通知、タイマーによる時間経過の通知、電源異常など。
- **内部割り込み**：プログラムの実行そのものに起因する割り込み。ゼロ除算、オーバーフロー、不正命令の実行など、プログラム自身のエラー（これを特に**プログラム割込み**と呼ぶこともある）に加え、後述する**スーパバイザコール**もこの内部割り込みの一種として扱われます。

### 1.5 NMI（ノンマスカブル割り込み）

割り込みの中には、ソフトウェア側の設定で「受け付けない（マスクする）」ことができるものと、できないものがあります。**NMI（Non-Maskable Interrupt）**は後者で、プログラム側の都合でマスク（無視）することができない割り込みです。電源異常やメモリの致命的なエラーなど、無視すると致命的な事態を招く重大な異常の通知に使われます。「マスク不可＝最優先で必ず処理される割り込み」という点が試験でも問われるポイントです。

### 1.6 スーパバイザコール（SVC）

アプリケーションプログラムは通常、ハードウェアに直接アクセスできないよう制限されています（ユーザーモード）。ファイル入出力など、OSの管理下にある特権的な処理を行いたいときは、プログラムが意図的に割り込みを発生させてOS（カーネル）に処理を依頼します。これが**スーパバイザコール（SVC）**です。異常発生時に受動的に起きる一般的な割り込みと異なり、SVCはプログラムが能動的に発生させる点が特徴で、内部割り込みの一種に分類されます。

### 1.7 PSW（プログラム状態語）

**PSW（Program Status Word）**は、プロセッサの動作状態（現在どの命令を実行中か、演算結果がゼロだったか／オーバーフローしたか、割り込みを許可する状態かどうか、ユーザーモードかスーパバイザモードか、など）をまとめて保持する特殊なレジスタです。割り込みが発生すると、後述するように現在のPSW（＝処理を中断した瞬間の状態）を退避し、割り込み処理終了後に元のPSWを復元することで、何事もなかったかのように元の処理を再開できます。

### 1.8 割り込み動作の流れ

割り込みが発生してから元の処理に戻るまでの一連の流れは、次のようになります。

1. 割り込み要因が発生する（外部割り込み・内部割り込み・SVCなど）。
2. 現在実行中の命令が完了する（または安全に中断できる）タイミングで、現在の**PSWとプログラムカウンタ（実行再開位置）を退避**する。
3. 割り込みの種類に応じた**割り込み処理ルーチン（割り込みハンドラ）**へ制御を移す。
4. 割り込み処理ルーチンが処理を実行する。
5. 処理完了後、退避しておいたPSWとプログラムカウンタを復元し、中断していた元の処理を再開する。

この「状態を退避→処理→復元」という一連の流れは、後述するマルチプロセッサやOSのタスク切り替え（コンテキストスイッチ）とも共通する重要な考え方です。

## 2. プロセッサの高速化方式

ここからは、上記の基本サイクルを「どう速くするか」という視点でのストーリーです。高速化には大きく分けて、①命令セットの設計思想を変える方向（CISC→RISC）と、②命令の処理そのものを並列化する方向（パイプライン、スーパスカラなど）の2つの流れがあります。

### 2.1 CISCとRISC：設計思想の対立

- **CISC（Complex Instruction Set Computer）**：1つの命令で複雑な処理（メモリアクセスを伴う演算など）を行えるようにした、命令の種類が多く高機能な命令セット。命令が複雑なため、その解読・実行には**マイクロプログラム方式（ストアドロジック方式）**が使われます。これは、1つの複雑な機械語命令を、あらかじめROMなどに格納された「マイクロ命令」の一連の実行手順（マイクロプログラム）に分解して実行する方式で、命令セットの変更・追加を（回路自体を変えずに）マイクロプログラムの書き換えで比較的柔軟に行えるのが利点です。
- **RISC（Reduced Instruction Set Computer）**：単純で実行時間が均一な命令に絞り込むことで、1命令あたりの実行速度を高め、後述するパイプライン処理を効率よく行えるようにした設計思想です。命令の解読を単純化できるため、マイクロプログラムのような間接的な実行方式ではなく、**結線論理（ワイヤードロジック）方式**、すなわち制御ロジックをハードウェア回路として直接組み込む方式が主に使われます。回路が単純な分、高速に動作しますが、命令セットの柔軟な変更は難しくなります。

CISCとRISCという対立軸、そしてそれぞれを支えるマイクロプログラム方式／結線論理方式という制御方式の対応関係は、試験でセットで問われる典型パターンです。

なお、**VLSI（Very Large Scale Integration：超大規模集積回路）**は、多数のトランジスタを1つの半導体チップ上に集積する技術そのものを指す用語で、CISC・RISCいずれの実現にも関わる、プロセッサの高集積化・高速化・小型化を支える基盤技術です。

### 2.2 パイプライン制御

RISCのように単純化された命令であっても、1命令ずつ「フェッチ→デコード→実行→…」を順番に完了させてから次の命令に進んでいては効率が悪いままです。そこで登場するのが**パイプライン制御**です。

パイプラインは、命令処理の各過程（**ステージ**：命令フェッチ、デコード、実行、書き戻しなど）を、まるで工場の流れ作業（ベルトコンベア）のように区切り、各ステージを専用の回路が担当します。1つの命令がステージ1を終えて次のステージ2に進んだら、すぐに次の命令をステージ1に投入する、という形で複数命令の処理を段階的に重ね合わせることで、見かけ上の処理速度（スループット）を大幅に向上させます。各ステージの処理にかかる時間の単位を**ピッチ**（クロック周期に相当）と呼びます。

### 2.3 パイプラインハザードと対策

パイプラインは理想的には毎ピッチ1命令ずつ完了させられますが、現実には命令同士の依存関係などにより流れが乱れることがあります。これを**パイプラインハザード**と呼び、代表的には次のような種類があります。

- **構造ハザード**：同じハードウェア資源（回路）を複数のステージが同時に使おうとして競合する。
- **データハザード**：ある命令の演算結果を、後続の命令がまだ書き戻される前に必要としてしまう（依存関係）。
- **制御ハザード（分岐ハザード）**：条件分岐命令の分岐先が確定するまで、次に読み込むべき命令が定まらない。

制御ハザードへの対策として重要なのが**投機実行**です。これは、分岐の判定結果が確定する前に「おそらくこちらだろう」という予測に基づいて先の命令を先行して実行してしまう方式です。予測が外れた場合は実行結果を破棄してやり直しますが、予測が当たれば待ち時間なくパイプラインを流し続けられるため、平均的な処理速度が向上します。同様の考え方に基づき、分岐先を先読みして命令をあらかじめ取得しておく制御を**先回り制御**と呼びます。

### 2.4 スーパスカラ方式とスーパパイプライン方式

パイプラインをさらに発展させた高速化方式として、次の2つがよく対比されます。

- **スーパスカラ方式**：パイプラインを複数本並列に用意し、依存関係のない複数の命令を同時（並行）に実行できるようにする方式。「幅」を広げるイメージです。
- **スーパパイプライン方式**：1本のパイプラインのステージ数をさらに細かく分割し、1ステージあたりの処理を軽くすることでクロック周波数を上げ、高速化する方式。「ステージを刻んで速く回す」イメージです。

両者は「並列に何本も流す（スーパスカラ）」か「1本をより細かく刻んで速く流す（スーパパイプライン）」かという、高速化のアプローチの違いとして対比的に覚えると整理しやすい論点です。

## 3. マルチプロセッサ

1つのプロセッサの中で高速化を突き詰めた次のステップが、**プロセッサを複数個搭載してシステム全体の処理能力を上げる**という発想、すなわち**マルチプロセッサシステム**です。

### 3.1 M.フリンの分類

複数のデータ・命令の流れをどう並列処理するかを整理した古典的な分類が、**M.フリンの分類（Flynnの分類）**です。命令の流れ（Instruction）とデータの流れ（Data）がそれぞれ単一（Single）か複数（Multiple）かで4分類します。

| 分類 | 意味 | 具体例 |
|---|---|---|
| **SISD** | 単一命令・単一データ | 従来の逐次処理コンピュータ |
| **SIMD** | 単一命令・複数データ | 同じ演算を大量データに同時適用（画像処理・ベクトル演算など） |
| **MISD** | 複数命令・単一データ | 実用例が少なく理論上の分類 |
| **MIMD** | 複数命令・複数データ | 一般的なマルチプロセッサ・マルチコアシステム |

現在のマルチプロセッサ／マルチコアシステムの多くは**MIMD**に分類されます。

### 3.2 密結合と疎結合

複数のプロセッサをどう接続・連携させるかによって、次の2種類に分けられます。

- **密結合マルチプロセッサ（TCMP：Tightly Coupled Multi-Processor）**：複数のプロセッサが**主記憶（メモリ）を共有**し、1つのOSが全プロセッサを一元的に管理する方式。プロセッサ間の通信が速く、負荷分散もしやすい一方、共有メモリへのアクセス競合がボトルネックになりやすい。
- **疎結合マルチプロセッサ（LCMP：Loosely Coupled Multi-Processor）**：各プロセッサがそれぞれ**独立したメモリ**を持ち、プロセッサごとに別のOSが動作して、ネットワークなどを介して通信・連携する方式。柔軟な拡張がしやすい一方、プロセッサ間通信のオーバーヘッドが大きい。

### 3.3 マルチプロセッサの隘路とアムダールの法則

プロセッサの数を増やせば増やすほど処理速度が比例して向上するわけではありません。共有資源（メモリやバス）へのアクセス競合、プロセッサ間の同期処理、そしてプログラム中に存在する「並列化できない逐次処理部分」などが性能向上の妨げとなります。こうした性能向上を頭打ちにする要因を**マルチプロセッサの隘路（あいろ）**と呼びます。

この限界を定量的に表した法則が**アムダールの法則**です。プログラム全体のうち並列化できない部分の割合をあらかじめ考慮すると、プロセッサ数をいくら増やしても得られる高速化率には理論的な上限があることを示します。具体的には、並列化可能な部分の割合を上げるほど高速化効果は大きくなりますが、たとえわずかでも逐次処理部分が残っていれば、プロセッサ数を無限に増やしても全体の高速化率はその逐次部分の割合で頭打ちになる、という考え方です。応用情報の午前試験では、この考え方に基づく高速化率の計算問題が定番として出題されます。

### 3.4 SMPとAMP

密結合マルチプロセッサの中でも、OSによるプロセッサの扱い方に応じてさらに次の2つに分類されます。

- **対称型マルチプロセッシング（SMP：Symmetric Multi-Processing）**：全プロセッサが対等な立場で、OS（**カーネル**）やタスクの管理を各プロセッサが同等に分担して実行する方式。現在の一般的なマルチコアPC・サーバの主流。
- **非対称型マルチプロセッシング（AMP：Asymmetric Multi-Processing）**：プロセッサごとに役割が固定されており、特定のプロセッサ（マスタ）がOSの制御を集中的に担当し、他のプロセッサ（スレーブ）は割り当てられた処理のみを実行する方式。

ここで登場する**カーネル**は、OSの中核部分であり、プロセスやメモリの管理、割り込みの処理（前章の内容とつながります）など、システムの根幹となる機能を担うプログラムです。SMPではこのカーネルの管理機能を複数プロセッサで共有・分担する点が特徴です。

## 4. 論理回路 ― プロセッサを支える最小単位

ここまでのプロセッサの働きは、突き詰めれば電気信号の「0」と「1」の組み合わせによる**論理回路**の集合によって実現されています。最後に、その基礎を確認します。

### 4.1 基本論理回路

論理回路の基本要素は、AND（論理積）、OR（論理和）、NOT（否定）の3つと、それらを組み合わせた**XOR（排他的論理和）**です。XORは「2つの入力が異なるときに1（真）を出力する」回路で、入力が一致すれば0を出力します。ANDやORと並んで、後述する加算器の構成要素として頻出します。

### 4.2 加算器

2進数の足し算をハードウェアで実現する回路が**加算器**です。

- **半加算器**：2つの1ビットの数（AとB）を加算し、和（S）と桁上げ（C：キャリー）を出力する回路。ただし下位の桁からの繰り上がり（キャリー入力）は考慮できない。SはA XOR B、CはA AND Bという単純な論理演算の組み合わせで構成できます。
- **全加算器**：半加算器に加えて、下位桁からの**桁上げ入力（キャリーイン）**も考慮して加算できる回路。半加算器を2つとOR回路を組み合わせることで構成できます。複数桁の2進数の加算では、下位からの桁上げを次の桁に伝える必要があるため、実用上は全加算器を連結して使います。

半加算器と全加算器の違い（桁上げ入力の有無）は、試験で図とともに問われる定番論点です。

### 4.3 フリップフロップ

論理回路がその瞬間の入力に対する出力を決めるだけの「組み合わせ回路」であるのに対し、**フリップフロップ**は1ビットの情報を**記憶（保持）**できる回路です。クロック信号などのタイミングに合わせて状態を保持・変化させることができ、レジスタや記憶装置など、状態を保持する必要があるあらゆる回路の最小単位として使われます。前章までのレジスタやPSWも、突き詰めればこのフリップフロップの集合として実装されています。

### 4.4 正論理と負論理

デジタル回路では電圧の高低（HighとLow）で0と1を表現しますが、どちらを「1（真）」に対応させるかには2通りの約束事があります。

- **正論理**：電圧が高い状態（High）を「1」、低い状態（Low）を「0」に対応させる。
- **負論理**：逆に、電圧が低い状態（Low）を「1」、高い状態（High）を「0」に対応させる。

同じ回路・同じ電圧の変化でも、正論理か負論理かによって「1」「0」の解釈が変わる、という点が問われるポイントです。

## 5. 試験対策まとめ（重要ポイント）

- **割り込みの分類**：外部割り込み（入出力・タイマーなど外的要因）と内部割り込み（プログラム自身のエラーやSVC）の区別、そして**NMIはマスク不可＝最優先**という性質は頻出。**PSW**は割り込み時に退避・復元される「処理状態そのもの」であることを押さえる。
- **CISC vs RISC**の対応関係：CISC＝命令が複雑・**マイクロプログラム方式（ストアドロジック）**、RISC＝命令が単純・**結線論理方式（ワイヤードロジック）**・パイプラインと相性が良い、というセットで暗記する。
- **パイプラインハザード**の3種類（構造・データ・制御）と、制御ハザードへの対策としての**投機実行**・**先回り制御**の関係を理解する。
- **スーパスカラ（並列に複数本）**と**スーパパイプライン（1本をより細かく分割）**の違いを対比的に押さえる。
- **Flynnの分類（SISD/SIMD/MISD/MIMD）**は表で丸暗記が効率的。現代のマルチコアは基本的にMIMD。
- **密結合（TCMP：メモリ共有・OS一元管理）**と**疎結合（LCMP：メモリ独立・OSも独立）**の違い、およびSMP（対等）とAMP（マスタ・スレーブ）の違いは対比で覚える。
- **アムダールの法則**：並列化できない逐次処理部分が性能向上の上限を決める、という考え方に基づく高速化率の計算問題は午前試験の定番。式を丸暗記するより「逐次部分の割合がボトルネックになる」という考え方を理解しておくと、初見の設問にも対応しやすい。
- **半加算器（桁上げ入力なし）と全加算器（桁上げ入力あり）**の違い、XOR・ANDによる構成は図で確認しておく。
- **正論理・負論理**は電圧の高低とビットの対応関係が逆転する、という定義の確認問題として出やすい。

---

*本ノートは「基本情報／応用情報技術者試験」の出題範囲（コンピュータ構成要素＞プロセッサ）に基づき作成しています。プロジェクト内の「出題傾向」「テキスト優先順位」ドキュメントによれば、支援士試験ではセキュリティ・ネットワークが最優先分野であり、本分野（コンピュータ構成要素）はA-1（共通知識）で1問前後の出題にとどまります。深追いはせず、本ノートの範囲で典型論点を押さえる学習配分が効率的です。*
